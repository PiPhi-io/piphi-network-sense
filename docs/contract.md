# Runtime Contract

Generated by `piphi-network-create`.

- Integration ID: `piphi-network-sense`
- Kind: `integration`
- Preset: `cloud-polling-api`
- Domain: `cloud-api`
- Port: `8090`

## Required Endpoints

- `GET /health`
- `GET /entities`
- `POST /command`
- `POST /config`
- `GET /ui-config`

## Automation Command Payload

`POST /command` accepts the Core automation runtime contract:

```json
{
  "contract_version": "automation.runtime.command.v1",
  "command": "refresh",
  "target": {
    "config_id": "sense-account",
    "device_id": "sense-account",
    "entity_id": null
  },
  "params": {},
  "capability": "device.refresh",
  "capability_requirements": ["device.refresh"]
}
```

Return `{"ok": true, ...}` for accepted commands. For validation failures, return `{"ok": false, "error": "unsupported_capability", "message": "..."}` with an appropriate 4xx status.

## Recommended Endpoints

- `GET /diagnostics`
- `POST /discover`
- `POST /config/sync`
- `POST /deconfigure`
- `GET /state`
- `GET /contract`
- `GET /events`
- `POST /telemetry/example`

Keep `manifest.json`, the contract source file, and route handlers in sync.

The Sense manifest additionally declares `config.maximum_instances: 1`. Core
should reject a second configuration with HTTP 409; the runtime enforces the
same limit if an older Core delivers more than one configuration.
