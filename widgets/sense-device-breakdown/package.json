{
  "name": "sense-device-breakdown",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp src/*.js dist/",
    "dev": "piphi-widget dev",
    "validate": "piphi-widget validate",
    "conformance": "piphi-widget conformance",
    "test": "node --test test/*.test.mjs",
    "pack": "piphi-widget pack"
  },
  "dependencies": {
    "piphi-network-widget-sdk": "^0.3.0"
  }
}
